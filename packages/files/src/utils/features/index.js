const { buildIdentifierObj } = require('./build-identifier-obj.helper');
const { generatePdfFile } = require('./get-file-generator.helper');

module.exports = {
  buildIdentifierObj,
  generatePdfFile,
};
