const alumnosAdapter = require('./alumnos/handlers');
const ciclosAdapter = require('./ciclosEscolares/handlers');
const gruposAdapter = require('./grupos/handlers');

module.exports = {
  alumnosAdapter,
  ciclosAdapter,
  gruposAdapter,
};
