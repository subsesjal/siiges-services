const { findAllGrados } = require('./find-all.handlers.grado.adapters');
const { findGroupGrados } = require('./find-group.handlers.grados-programas.adapters');

module.exports = {
  findAllGrados,
  findGroupGrados,
};
