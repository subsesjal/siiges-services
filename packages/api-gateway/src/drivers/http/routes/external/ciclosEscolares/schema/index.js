const { findAllCiclosEscolares } = require('./find-all.ciclos-escolares.schema');
const { createCicloEscolar } = require('./create.ciclo-escolar.schema');

module.exports = {
  findAllCiclosEscolares,
  createCicloEscolar,
};
