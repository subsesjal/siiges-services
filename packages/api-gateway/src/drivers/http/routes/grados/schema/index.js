const { findAllGradosSchema } = require('./find-all.grados.schemas');
const { findGroupGradosSchema } = require('./find-group.grados-programas.schemas');

module.exports = {
  findAllGradosSchema,
  findGroupGradosSchema,
};
