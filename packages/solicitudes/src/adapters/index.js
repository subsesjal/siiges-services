const diligencia = require('./diligencia.adapters');
const institucion = require('./institucion.adapters');
const representante = require('./representante.adapters');

module.exports = {
  diligencia,
  institucion,
  representante,
};
